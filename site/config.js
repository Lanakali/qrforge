/* QRForge site configuration (static build — this file ships with GitHub Pages).
 *
 * Point `apiBase` at your hosted QRForge API once it's deployed (see README →
 * "Deployment"). Leave it empty to run in demo mode: the demo and dashboard
 * QR generator then run entirely client-side and no API calls are made.
 */
window.QRFORGE_CONFIG = {
  apiBase: "", // e.g. "https://qrforge-api.yourdomain.com"
};
